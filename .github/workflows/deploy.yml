name: sosmecanica deployment

on:
  push:
    branches: [master]

  pull_request:
    branches: [master]

  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Repo Checkout
        uses: actions/checkout@v4

      - name: Build
        run: echo "Building the app"

  deploy-production:
    # Só roda em push direto na master ou disparo manual — NUNCA em pull_request
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'

    runs-on: ubuntu-latest

    environment:
      name: production
      url: ${{ vars.URL }}

    needs: build

    steps:
      - name: Repo Checkout
        uses: actions/checkout@v4

      - name: Start SSH Agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SERVER_SSH_KEY }}
          ssh-passphrase: ${{ secrets.SERVER_SSH_PASSPHRASE }}

      - name: Configure known_hosts (evita MITM)
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          # SERVER_SSH_HOST_KEY deve conter a saída de:
          #   ssh-keyscan -p <porta> <host>
          # gerada uma única vez de um canal confiável e salva como secret.
          echo "${{ secrets.SERVER_SSH_HOST_KEY }}" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Create Exclude File
        run: |
          echo ".git/" > .rsync-exclude
          echo "node_modules/" >> .rsync-exclude
          echo "storage/" >> .rsync-exclude
          echo ".env" >> .rsync-exclude
          echo "vendor/" >> .rsync-exclude
          echo "public/build/" >> .rsync-exclude
          git ls-files --ignored --exclude-standard -o >> .rsync-exclude

      - name: Deploy files
        run: |
          rsync -avz \
            --delete \
            --exclude-from='.rsync-exclude' \
            --update \
            -e "ssh -p ${{ vars.REMOTE_PORT }} -o UserKnownHostsFile=~/.ssh/known_hosts -o StrictHostKeyChecking=yes" \
            ./ \
            ${{ vars.REMOTE_USER }}@${{ vars.REMOTE_HOST }}:${{ vars.TARGET }}/

      - name: Deploy application
        run: |
          ssh \
            -p ${{ vars.REMOTE_PORT }} \
            -o UserKnownHostsFile=~/.ssh/known_hosts \
            -o StrictHostKeyChecking=yes \
            ${{ vars.REMOTE_USER }}@${{ vars.REMOTE_HOST }} << 'EOF'

          set -e

          cd ${{ vars.TARGET }}

          echo "Building Docker images (sem substituir containers ainda)..."
          docker compose build

          echo "Subindo container temporário para instalar dependências e migrar..."
          docker compose run --rm laravel.sosmecanica composer install \
            --no-interaction \
            --prefer-dist \
            --optimize-autoloader

          echo "Running database migrations..."
          docker compose run --rm laravel.sosmecanica php artisan migrate --force

          echo "Subindo/recriando containers com a nova imagem..."
          docker compose up -d

          echo "Optimizing Laravel..."
          docker compose exec -T laravel.sosmecanica php artisan optimize

          echo "Deployment completed successfully."
          EOF